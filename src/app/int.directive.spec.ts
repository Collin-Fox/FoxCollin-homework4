import { IntDirective } from './int.directive';

describe('IntDirective', () => {
  it('should create an instance', () => {
    const directive = new IntDirective();
    expect(directive).toBeTruthy();
  });
});
